spring:
  application:
    name: eureka_client_feign_client



server:
  port: 0

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka


animal:
  service: eureka-client-animals-service